<div class="flex gap-4">
  <label>
    <input type="checkbox" class="peer">
    <%= render(Form::SwitchComponent.new) do |switch| %>
      <% switch.title { "Title" } %>
    <% end %>
  </label>
  <label>
    <input type="checkbox" class="peer" checked>
    <%= render(Form::SwitchComponent.new) do |switch| %>
      <% switch.title { "Title" } %>
    <% end %>
  </label>
</div>
