<%= form_tag unread_entries_entry_path(entry.id), remote: true, data: {behavior: 'toggle_read', entry_id: entry.id, feed_id: entry.feed_id, published: entry.published.to_i}, class: 'entry-button-wrap' do %>
  <button class="entry-button button-toggle-read" title="Mark as read/unread">
    <% if read %>
      <span class="circle-empty"></span>
    <% else %>
      <span class="circle-filled"></span>
    <% end %>
  </button>
<% end %>
