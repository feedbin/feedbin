<div class="service-options-wrap">
    <div class="service-options">
        <div class="clearfix">
            <div class="half">
                <%= form_for(record) do |f| %>
                    <%= f.hidden_field :service_id, value: sharing_service[:service_id] %>
                    <%= f.hidden_field :operation, value: '' %>
                    <%= f.url_field :webhook_url, placeholder: 'URL' %>

                    <%= submit_tag "Save", class: 'button button-primary' %>
                    <%= button_tag 'Cancel', class: 'button button-cancel button-inline', data: {behavior: "hide_service_options"} %>
                <% end %>
            </div>
            <div class="half">
                <small class="share-help">Feedbin will send an HTTP POST request to the specified URL. The body will contain a JSON encoded version of the article.  <a href="https://github.com/feedbin/feedbin-api/blob/master/content/entries.md#sample-responses">Here's an example</a> of what data to expect.</small>
            </div>
        </div>
    </div>
</div>
