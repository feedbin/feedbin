<div class="service-options-wrap">
    <div class="service-options">
        <div class="clearfix">
            <div class="half">
                <%= form_for(record) do |f| %>
                    <%= f.hidden_field :service_id, value: sharing_service[:service_id] %>
                    <%= f.hidden_field :operation, value: 'authorize' %>
                    <%= password_field_tag 'password', '', placeholder: 'API Token', class: 'no-margin' %>
                    <div class="button-wrap">
                        <%= button_tag 'Cancel', type: "button", class: 'button button-secondary button-inline', data: {behavior: "hide_service_options"} %>
                        <%= submit_tag "Save", class: 'button button-primary-plain' %>
                    </div>
                <% end %>
            </div>
            <div class="half">
                <small class="share-help">You can find your API token on your <a href="https://pinboard.in/settings/password" target="_blank" rel="noopener noreferrer">Pinboard Password page</a>.</small>
            </div>
        </div>
    </div>
</div>
