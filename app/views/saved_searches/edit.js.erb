if ("updateDialog" in feedbin) {
  feedbin.updateDialog("<%= Dialog::EditSavedSearch.dom_id %>", "<%= j render Dialog::EditSavedSearch.new(saved_search: @saved_search) %>");
} else {
  feedbin.replaceModal("modal-purpose-edit", "<%= j render SavedSearches::FormComponent.new(saved_search: @saved_search) %>");
}

