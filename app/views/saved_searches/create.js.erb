<%= render partial: 'shared/update_feeds', locals: {feed_data: @feed_data} %>
element = $("[data-saved-search-id=<%= @saved_search.id %>] a.feed-link");
$('.modal').modal('hide');
feedbin.reveal(element);
