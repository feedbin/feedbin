<%= render partial: 'shared/update_feeds', locals: {feed_data: @feed_data} %>
$('.saved-search-wrap').removeClass('show');
$('#query').val('');
$("[data-saved-search-id=<%= @saved_search.id %>] a.feed-link").click();
$('.modal').modal('hide')