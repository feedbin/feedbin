<% @favicons.each do |favicon| %>
<% if favicon.favicon.present? %>
.favicon-<%= favicon.host.parameterize %> {
    background-image: url("data:image/png;base64,<%= favicon.favicon %>");
    background-color: transparent !important;
}
<% end %>
<% end %>
