if ("updateDialog" in feedbin) {
  feedbin.updateDialog("<%= Dialog::SocialReplies.dom_id %>", "<%= j render Dialog::SocialReplies.new(posts: @microposts) %>");
} else {
  feedbin.modalContent("modal-purpose-conversation", "<%= j render partial: 'thread', locals: {microposts: @microposts} %>");
}

feedbin.entryTime();
feedbin.audioVideo("general_modal");
feedbin.formatImages();
