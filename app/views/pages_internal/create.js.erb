<% if @entry %>
    <%= render partial: 'shared/update_feeds', locals: {feed_data: @feed_data} %>
    feedbin.showNotification('Page saved!');
<% else %>
    feedbin.showNotification('Unable to save page', true);
<% end %>
