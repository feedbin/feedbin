feedbin.tagVisibility(<%= raw json_escape(@user.tag_visibility.to_json) %>);
<%= render partial: 'shared/update_feeds', locals: {feed_data: @feed_data} %>
$('[data-behavior~=feeds_target] [data-tag-id~=<%= @new_tag.id %>] .tag-link')[0]?.click()
