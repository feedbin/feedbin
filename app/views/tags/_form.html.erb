<div class="modal-dialog" role="document">
    <%= form_for(tag, remote: true, method: :patch, html: {class: 'settings tags-form', data: {behavior: "disable_on_submit"}}) do |f| %>
        <% content_for :modal_content do %>
            <div class="modal-header-input">
                <%= f.text_field :name, placeholder: tag.name %>
                <span class="icon">📦</span>
            </div>
        <% end %>

        <% content_for :modal_footer do %>
            <%= link_to "Delete", tag_path(tag), method: :delete, remote: true, class: "button-text settings-unsubscribe", data: { confirm: "Are you sure you want to delete this tag?" } %>
            <input type="submit" class="button" value="Save">
        <% end %>

        <%= render template: "layouts/modal_inner", locals: {purpose: "edit"} %>
    <% end %>
</div>
