<% if action.valid? %>
    $('body').append("<%= j render Actions::PreviewModalComponent.new(action: action) %>");
    feedbin.showModal("action_preview");
<% else %>
    feedbin.showNotification('Invalid action: <%= j action.errors.full_messages.join('. ') %>', true);
<% end %>
