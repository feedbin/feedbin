If a new article is published

<% if action.query.present? %>
  that matches the search <strong><%= action.query %></strong>
<% end %>

<% if action.all_feeds %>
  in <strong>any</strong> feed
<% elsif action.feed_ids.reject{ |feed_id| feed_id.empty? }.any? %>
  in the feed <strong><%= action_feed_names(action) %></strong>
<% end %>

then <%= action_names(action) %>.