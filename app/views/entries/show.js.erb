feedbin.updateEntryContent('<%= j render partial: "show", locals: { entry: @entry, feed: @feed, content_view: @content_view, services: @services } %>');

feedbin.localizeTime($('[data-behavior~=entry_content_target]'))

feedbin.applyUserTitles()
