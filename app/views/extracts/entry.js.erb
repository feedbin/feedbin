feedbin.extractCache["<%= @entry.id %>"] = "<%= j render partial: "entry", locals: {content: @content, content_info: @content_info, entry: @entry, user: @user} %>";
$('[data-entry-id=<%= @entry.id %>] [data-content-option=extract]').html(feedbin.extractCache["<%= @entry.id %>"]);
feedbin.changeContentView('extract')
feedbin.formatEntryContent(<%= @entry.id %>, false, false);
feedbin.entryTime();
