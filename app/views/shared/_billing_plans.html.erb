<%
  plan_data = plans.map do |plan|
    {id: plan.id, name: plan.name, amount: plan.price_in_cents}
  end
%>
<script>
    $(function() {
        new feedbin.Payments(<%= raw json_escape(plan_data.to_json) %>, <%= default_plan.id %>);
    })
</script>
