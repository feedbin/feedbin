<% if current_user.present? %>
<script>
jQuery(function() {
    $("<link>")
        .appendTo('head')
        .attr({rel: "stylesheet"})
        .attr({href: "<%= url_for(favicons_path(hash: current_user.favicon_hash || 'none', format: :css)) %>"});
});
</script>
<% end %>
