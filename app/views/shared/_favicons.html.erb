<% if current_user.present? %>
<script>
jQuery(function() {
    $("<link>")
        .appendTo('head')
        .attr({rel: "stylesheet"})
        .attr({href: "<%= url_for(favicons_path(hash: current_user.get_favicon_hash, format: :css)) %>"});
});
</script>
<% end %>
