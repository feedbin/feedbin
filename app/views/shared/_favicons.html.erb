<% if current_user %>
<script>
jQuery(function() {
    $("<link>")
        .appendTo('head')
        .attr({rel: "stylesheet"})
        .attr({href: "<%= url_for(favicons_path(format: :css)) %>"});
});
</script>
<% end %>
