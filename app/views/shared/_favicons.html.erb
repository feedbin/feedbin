<% if current_user %>
<script>
jQuery(function() {
    $("<link>")
        .appendTo('head')
        .attr({rel: "stylesheet"})
        .attr({href: "<%= stylesheet_path(url_for(favicons_path(hash: current_user.favicon_hash || 'none', format: :css))) %>"});
});
</script>
<% end %>
