<% content_for :modal_content, "", flush: true %>

<% content_for :modal_title, flush: true do %>
    Tag Feed
<% end %>

<% content_for :modal_body, flush: true do %>
    <%= render partial: "shared/loading_content_placeholder" %>
<% end %>

<% content_for :modal_footer, flush: true do %>
    <button type="button" class="button small" data-behavior="submit_tags">Update</button>
<% end %>

<%= render template: "layouts/modal", locals: {purpose: "tags"} %>
