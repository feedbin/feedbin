<% content_for :modal_content, "", flush: true %>

<% content_for :modal_title, flush: true do %>
    Settings
<% end %>

<% content_for :modal_body, flush: true do %>
<div data-nav="modal" class="group">
    <%= render Shared::SettingsNavView.new(user: current_user) %>
</div>
<% end %>

<% content_for :modal_footer, "", flush: true %>

<%= render template: "layouts/modal", locals: {purpose: "settings_nav", classes: "modal-lg"} %>
