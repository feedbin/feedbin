<%= form_with(model: current_user, url: format_user_path(current_user), html: { class: 'hide', data: {behavior: 'resizable_form', remote: true} }) do |f| %>
    <%= hidden_field_tag 'column_widths', 'column_widths' %>
    <%= hidden_field_tag 'column', '' %>
    <%= hidden_field_tag 'width', '' %>
<% end %>
