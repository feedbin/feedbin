<%= render partial: 'shared/update_feeds', locals: {feed_data: @feed_data} %>
$('.feed_tag_list').autocomplete('close');
feedbin.data.tagNames = <%= raw json_escape(@user.tag_names.to_json) %>;
$('.modal').modal('hide')
