<% if @tumblr_hosts.any? %>
$('.tumblr-hosts-placeholder').html("<%= j options_for_select(@tumblr_hosts) %>");
<% end %>

<% if @evernote_notebooks.any? %>
$('.evernote-notebooks-placeholder').html("<%= j options_for_select(@evernote_notebooks, @evernote_selected) %>");
<% end %>